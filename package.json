{
  "name": "spera-agent-plugin",
  "version": "1.0.0",
  "private": true,
  "description": "Validation and smoke tests for the portable Spera Agent Plugin and its client adapters.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node scripts/validate-plugin.mjs",
    "test:live": "node scripts/smoke-live.mjs",
    "validate:submission": "node scripts/validate-plugin.mjs --submission"
  }
}
